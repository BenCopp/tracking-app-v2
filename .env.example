# No runtime environment variables are required for Firebase-only operation.
# Firebase config is loaded from firebase-applet-config.json.
# If you add runtime env variables later, document them here.
